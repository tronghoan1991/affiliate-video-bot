# requirements_colab.txt — Chạy trên Google Colab (GPU)
# Cài đặt theo thứ tự: PyTorch trước, sau đó file này

# ── Server & tunnel ───────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.32.0
pyngrok>=7.2.0
httpx>=0.27.0

# ── Telegram ──────────────────────────────────────────────────
python-telegram-bot==21.5

# ── Diffusers (Wan2.1 cần >= 0.33.0, cài từ source trước) ───
# pip install git+https://github.com/huggingface/diffusers
transformers>=4.46.0
accelerate>=1.1.0
huggingface-hub>=0.25.0
safetensors>=0.4.5
imageio>=2.35.0
imageio-ffmpeg>=0.5.1
einops>=0.7.0
ftfy>=6.1.1

# ── Real-ESRGAN ───────────────────────────────────────────────
basicsr>=1.4.2
realesrgan>=0.3.0
facexlib>=0.3.0

# ── Google Drive ──────────────────────────────────────────────
google-api-python-client>=2.140.0
google-auth>=2.35.0
google-auth-httplib2>=0.2.0

# ── HF Spaces fallback ────────────────────────────────────────
gradio-client>=1.3.0

# ── Image & Vision ────────────────────────────────────────────
Pillow>=10.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# ── Misc ──────────────────────────────────────────────────────
python-dotenv>=1.0.0
