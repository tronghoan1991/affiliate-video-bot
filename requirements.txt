# ============================================================
# requirements.txt — Affiliate Video Bot v4.0
# Engine chính: Wan2.1-I2V-14B (miễn phí, chất lượng cao)
# ============================================================

# ── Telegram ──────────────────────────────────────────────────
python-telegram-bot==21.5

# ── PyTorch + CUDA (cài riêng trước, xem DEPLOY_GUIDE.md) ────
# pip install torch==2.4.0 torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.4.0
torchvision>=0.19.0

# ── Diffusers (Wan2.1 + AnimateDiff) ─────────────────────────
# Wan2.1 yêu cầu diffusers >= 0.33.0 (cài từ source)
# pip install git+https://github.com/huggingface/diffusers
diffusers>=0.33.0
transformers>=4.46.0
accelerate>=1.1.0
huggingface-hub>=0.25.0
safetensors>=0.4.5
imageio>=2.35.0
imageio-ffmpeg>=0.5.1
einops>=0.7.0
omegaconf>=2.3.0
ftfy>=6.1.1

# ── Real-ESRGAN ───────────────────────────────────────────────
basicsr>=1.4.2
realesrgan>=0.3.0
facexlib>=0.3.0
gfpgan>=1.3.8

# ── Google Drive API ──────────────────────────────────────────
google-api-python-client>=2.140.0
google-auth>=2.35.0
google-auth-httplib2>=0.2.0

# ── HF Spaces cloud fallback ──────────────────────────────────
gradio-client>=1.3.0
httpx>=0.27.0

# ── Computer Vision & Image ───────────────────────────────────
Pillow>=10.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0

# ── Utilities ─────────────────────────────────────────────────
python-dotenv>=1.0.0
tqdm>=4.66.0
